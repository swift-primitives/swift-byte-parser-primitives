# ``Byte_Parser_Primitives``

@Metadata {
    @DisplayName("Byte Parser Primitives")
    @TitleHeading("Swift Primitives")
}

Byte-domain parser combinators: `Byte.Parser` matches a single byte and `Byte.Literal.Parser` matches a fixed byte sequence against a streaming byte input — the byte layer over the input-agnostic combinator algebra in `swift-parser-primitives`.

## Topics

### Byte parsers

- ``Byte/Parser``
- ``Byte/Literal/Parser``
```
